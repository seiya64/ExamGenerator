# Task 11: Add Create Exam Route Placeholder

## Goal

Create the `/exams/new` route with a simple status screen before implementing the full form.

## Implementation

- Add `/exams/new` route.
- Add a placeholder component with translated status text, for example `Create exam route`.
- Update the exams list create button so it navigates to `/exams/new`.
- Keep the placeholder deployable and simple.

## Acceptance Checks

- `/exams/new` shows the placeholder.
- `/exams` create button navigates to `/exams/new`.
- Language switching still works.
- `npm run build` passes.

## Suggested Commit

`feat: add create exam route placeholder`
